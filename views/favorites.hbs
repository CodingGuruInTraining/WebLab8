<h1>Favorites</h1>
<!--Creates a new div element for each favorites item.-->
{{#each favorites}}
    <div class="favitem" id="{{this.date}}">
        <a href="{{this.nasa_url}}">
            <img class="favimage" src="{{this.url}}">
        </a>
        <p class="favtitle">{{this.title}}</p>
        <p class="favdate">{{this.date}}</p>
        <form action="favorites/delete" method="post">
            <button type="submit" id="delete">Remove</button>
            <input type="hidden" name="date" value="{{this.date}}">
        </form>
    </div>
{{else}}
    <p>No favorites saved yet.</p>
{{/each}}
<!--Displays navigation buttons.-->
{{> navigation}}